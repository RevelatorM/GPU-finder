package main

import "fmt"

//===================
type GPU struct {
	Name string
	VRAM int
	MHz  float32
	vmhz float32
}

//===================

func main() {
	for {
		fmt.Print("Welcome to GPU finder,type in GPU`s name to find: \n")

	}
}
